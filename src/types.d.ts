/**
 * Ambient Workers runtime types come from `@cloudflare/workers-types` (v5+).
 * Keep this file empty of conflicting globals so consumers and this package
 * share one source of truth. Prefer `wrangler types` in apps for Env +
 * compatibility-date-locked runtime types.
 */

export {};
